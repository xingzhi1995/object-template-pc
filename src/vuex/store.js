import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const actions = {
}
const mutations = {
}
const state = {
}
// getters 只会依赖 state 中的成员去更新
const getters = {
}

const store = new Vuex.Store({
  actions,
  mutations,
  state,
  getters
})
export default store
